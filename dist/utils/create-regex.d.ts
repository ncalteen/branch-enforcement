/**
 * Creates a regex from a string.
 *
 * @param ref The string to create a regex from.
 * @returns A regex created from the string.
 */
export declare function createRegex(ref: string): Promise<RegExp>;
